*quickmate.txt*        quickmate.nvim help        Last change: 2026-03-01

==============================================================================
CONTENTS                                                  *quickmate-contents*

1. Introduction...........................|quickmate-introduction|
2. Requirements...........................|quickmate-requirements|
3. Installation...........................|quickmate-installation|
4. Setup..................................|quickmate-setup|
5. Commands...............................|quickmate-commands|
6. API....................................|quickmate-api|
7. Parsers................................|quickmate-parsers|
8. Presets................................|quickmate-presets|
9. Notifications..........................|quickmate-notifications|
10. Health................................|quickmate-health|
11. Versioning............................|quickmate-versioning|
12. Testing...............................|quickmate-testing|

==============================================================================
INTRODUCTION                                              *quickmate-introduction*

quickmate.nvim is a quickfix-first command runner for Neovim.

Flow:
  command -> parser -> quickfix

It runs checks asynchronously with |vim.system()|, parses output from common
linters/typecheckers/analyzers, and writes normalized quickfix entries.

Note: many LSPs can populate quickfix directly via
|vim.diagnostic.setqflist()|. In many TypeScript/script-driven setups, LSP
diagnostics are limited to open files, so explicit project commands (for
example tsc/nuxt/eslint/oxlint) are necessary for full-project results.
quickmate normalizes that output into one quickfix workflow. This keeps checks
repeatable and independent of local Neovim LSP setup.

Main help tag:                                              *quickmate.nvim*

==============================================================================
REQUIREMENTS                                              *quickmate-requirements*

- Neovim 0.10+

==============================================================================
INSTALLATION                                              *quickmate-installation*

With lazy.nvim:

>lua
  {
    'Aietes/quickmate.nvim',
    opts = {},
  }
<

After installation, docs are available with:
  :help quickmate.nvim

==============================================================================
SETUP                                                            *quickmate-setup*

Call setup once:

>lua
  require('quickmate').setup({
    open_quickfix = 'on_items',
    quickfix_view = 'quickfix',
    default_errorformat = vim.o.errorformat,
    commands = true,
    package_manager = nil,
    package_manager_priority = { 'pnpm', 'bun', 'npm', 'yarn' },
    presets = {
      check = {
        cmd = 'pnpm run check',
        parser = 'mixed_lint_json',
        title = 'project check',
      },
    },
  })
<

Options:                                          *quickmate-setup-options*
  open_quickfix                                           (default: 'on_items')
    One of: 'on_items' | 'always' | 'never'

  quickfix_view                                          (default: 'quickfix')
    One of: 'quickfix' | 'trouble'
    With 'trouble', results open in trouble.nvim's quickfix mode instead of
    the quickfix window. The quickfix list is still populated either way.
    Falls back to the quickfix window (with a one-time warning) when
    trouble.nvim is not installed.

  default_errorformat                              (default: |'errorformat'|)
    String used by efm fallback parser.

  commands                                                   (default: true)
    When true, registers |:Check|, |:CheckScript|, |:CheckPreset|.

  package_manager                                             (default: auto)
    One of: 'pnpm' | 'bun' | 'npm' | 'yarn' | nil

  package_manager_priority
    Detection fallback order.
    Default: { 'pnpm', 'bun', 'npm', 'yarn' }

  presets
    Table of preset definitions merged with built-ins.

==============================================================================
COMMANDS                                                      *quickmate-commands*

:Check {shell command}                                           *:Check*
    Runs any shell command through quickmate.
    Example: >
      :Check cargo clippy --message-format=json
<

:Check @{preset}                                                  *:Check-@*
    Shorthand for running a preset.
    Example: >
      :Check @rust
<

:CheckScript {script-name}                                   *:CheckScript*
    Runs package manager script with automatic manager detection.
    Default parser: mixed_lint_json

:CheckPreset {name}                                           *:CheckPreset*
    Runs a named preset directly.

==============================================================================
API                                                                  *quickmate-api*

Require module:
>lua
  local check = require('quickmate')
<

Functions:

check.setup(opts)                                           *quickmate.setup()*
check.version()                                         *quickmate.version()*
check.run(cmd, opts?)                                         *quickmate.run()*
check.run_script(name, opts?)                           *quickmate.run_script()*
check.run_preset(name, opts?)                           *quickmate.run_preset()*
check.register_parser(name, fn)                     *quickmate.register_parser()*
check.register_preset(name, preset)                 *quickmate.register_preset()*

Also available:
  check.VERSION

run() options:
  title?: string
  cwd?: string
  env?: table<string, string>
  timeout_ms?: number
  package_manager?: 'pnpm' | 'bun' | 'npm' | 'yarn'
  parser?: string | function(ctx): parser_result
  errorformat?: string
  open_quickfix?: 'on_items' | 'always' | 'never'
  quickfix_view?: 'quickfix' | 'trouble'
  on_complete?: function(result)

on_complete(result) fields:
  cmd, title, code, signal, stdout, stderr, combined,
  items, parser_used, duration_ms

==============================================================================
PARSERS                                                          *quickmate-parsers*

Built-in parsers:
  efm                                                     *quickmate-parser-efm*
  oxlint_json                                       *quickmate-parser-oxlint_json*
  eslint_json                                       *quickmate-parser-eslint_json*
  cargo_json                                         *quickmate-parser-cargo_json*
  selene                                                 *quickmate-parser-selene*
  selene_json2                                     *quickmate-parser-selene_json2*
  luacheck                                           *quickmate-parser-luacheck*
  luacheck_text                                 *quickmate-parser-luacheck_text*
  ts_text                                               *quickmate-parser-ts_text*
  mixed_lint_json                               *quickmate-parser-mixed_lint_json*
  oxlint                                                   *quickmate-parser-oxlint*
  eslint_text                                       *quickmate-parser-eslint_text*
  eslint                                                   *quickmate-parser-eslint*

Parser fallback strategy:
  1. Explicit parser in opts.parser
  2. Auto parser by command pattern
  3. efm fallback parser (uses errorformat)

Custom parser contract:
  Input ctx:
    cmd, title, cwd, stdout, stderr, combined, errorformat
  Return:
    { items = vim.quickfix.entry[], ok? = boolean, message? = string }
  Returning nil or ok=false triggers fallback parser.

==============================================================================
PRESETS                                                          *quickmate-presets*

Built-in presets:
  oxlint  eslint  clippy  rust  tsc  nuxt  lua  selene  luacheck

Run preset:
  :CheckPreset oxlint
  :Check @rust

Preset fields:
  cmd: string | function({ package_manager, cwd }): string
  title?: string
  parser?: string | function(ctx): parser_result
  errorformat?: string
  cwd?: string
  env?: table<string, string>
  timeout_ms?: integer
  open_quickfix?: 'on_items' | 'always' | 'never'
  quickfix_view?: 'quickfix' | 'trouble'

==============================================================================
NOTIFICATIONS                                            *quickmate-notifications*

quickmate uses |vim.notify()| for:
  - running spinner
  - no issues / issues found summaries
  - parser fallback warnings
  - execution/parser errors

==============================================================================
HEALTH                                                            *quickmate-health*

Run:
>
  :checkhealth quickmate
<

Health output includes:
  - Neovim version compatibility
  - shell configuration
  - package manager executables on PATH
  - parser/preset registration state in current session

==============================================================================
VERSIONING                                                      *quickmate-versioning*

quickmate.nvim uses SemVer with Git tags.

Recommended tag format:
  vX.Y.Z

Runtime version:
>
  :lua print(require('quickmate').VERSION)
  :lua print(require('quickmate').version())
<

Release script:
>
  ./scripts/release.sh 0.1.1
<
This updates VERSION files, runs tests, creates a release commit, and tags it.

==============================================================================
TESTING                                                          *quickmate-testing*

Run tests:
>
  ./scripts/test.sh
<

==============================================================================
vim:tw=78:ts=8:noet:ft=help:norl:
